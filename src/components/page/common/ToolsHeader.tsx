"use client";

import { ToolsProductHeader } from "features/tools";

export const TOOLS_HEADER_OFFSET_HEIGHT = { base: 20, sm: 24 };

export default function ToolsHeader() {
  return <ToolsProductHeader />;
}